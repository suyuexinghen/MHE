# ADR-001: Use `metaharness` package name

## Status
Accepted

## Decision
Use `metaharness` as the Python package name inside `MHE/src/`.

## Rationale
The wiki is the design authority and consistently uses `metaharness` naming. Adopting that spelling reduces translation churn between design docs and implementation.
